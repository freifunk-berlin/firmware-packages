#nodefault 'web-wizard'
nodefault 'freifunk-berlin-defaults'
nodefault 'freifunk-berlin-utils'
nodefault 'freifunk-berlin-ui'
nodefault 'freifunk-berlin-wizard'
nodefault 'freifunk-berlin-notunnel'
nodefault 'freifunk-berlin-mesh-olsr'
nodefault 'freifunk-berlin-statistics-collectd'
nodefault 'freifunk-berlin-mesh-bmx'
nodefault 'freifunk-berlin-batman'
nodefault 'freifunk-berlin-owm'

packages 'freifunk-berlin-defaults' \
	freifunk-berlin-dhcp-defaults \
	freifunk-berlin-firewall-defaults \
	freifunk-berlin-freifunk-defaults \
	freifunk-berlin-network-defaults \
	freifunk-berlin-statistics-defaults \
	freifunk-berlin-system-defaults

packages 'freifunk-berlin-utils' \
	mtr \
	iperf \
	tmux \
	vnstat \
	ethtool \
	tcpdump

packages 'freifunk-berlin-ui' \
	-luci-mod-failsafe \
	freifunk-berlin-uhttpd-defaults \
	uhttpd \
	uhttpd-mod-ubus \
	px5g-mbedtls \
	luci-mod-admin-full \
	luci-theme-bootstrap

packages 'freifunk-berlin-wizard' \
	luci-app-ffwizard-berlin \
	freifunk-berlin-migration

#packages 'uplink-notunnel' \
#	freifunk-berlin-uplink-notunnel

packages 'freifunk-berlin-mesh-olsr' \
	freifunk-berlin-olsrd-defaults \
	olsrd \
	olsrd-mod-arprefresh \
	olsrd-mod-dyn-gw \
	olsrd-mod-jsoninfo \
	olsrd-mod-txtinfo \
	olsrd-mod-nameservice \
	olsrd-mod-watchdog \
	kmod-ipip

packages 'freifunk-berlin-ui & freifunk-berlin-mesh-olsr' \
	luci-app-olsr

packages 'freifunk-berlin-statistics-collectd' \
	luci-app-statistics \
	collectd \
	collectd-mod-interface \
	collectd-mod-iwinfo \
	collectd-mod-network \
	collectd-mod-rrdtool \
	collectd-mod-ping \
	collectd-mod-uptime \
	collectd-mod-memory \
	freifunk-berlin-statistics-defaults

packages 'freifunk-berlin-mesh-olsr & freifunk-berlin-statistics-collectd' \
	collectd-mod-olsrd

packages 'freifunk-berlin-mesh-bmx' \
	luci-app-bmx7

packages 'freifunk-berlin-batman' \
	kmod-batman-adv \
	alfred \
	batctl-default

packages 'freifunk-berlin-owm' \
	luci-app-owm \
	luci-app-owm-ant \
	luci-app-owm-cmd \
	luci-app-owm-gui

